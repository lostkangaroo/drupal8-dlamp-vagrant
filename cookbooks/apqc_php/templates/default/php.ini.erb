<%
def apqc_php_ini_property(value)
  case value
  when "On", "Off", "True", "False", "Yes", "No", "None"
    return value
  when Fixnum
    return value
  else
    return "\"#{value}\""
  end
end
%>
[PHP]
; Generated by Chef
; Local modifications will be overwritten.
; Documentation of directives can be found here: http://php.net/manual/ini.php

<% @settings.keys.sort.each do |heading| %>
[<%= heading %>]
<% @settings[heading].each do |key, value| %>
<%= key %> = <%= apqc_php_ini_property(value) %>
<% end %>
<% end %>